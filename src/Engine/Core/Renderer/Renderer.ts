import { BaseObject } from "../BaseObject";
import { GameObject } from "../GameObject";

export class Renderer extends BaseObject {
    /**
     * The method used to draw on screen.
     * @param gameObject
     * @constructor
     */
    Draw(gameObject: GameObject): void {
    }
}